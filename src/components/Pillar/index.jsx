export const Pillar = (props) => {
    return (
        <div class="pilar">
            <strong class="pilar__titulo">{props.titulo}</strong>
            <p class="pilar__parrafo">{props.parrafo}</p>
        </div>
    )
}